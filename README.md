# fraud-detection
This project detect if the fraud has happen or not with in a transaction using Graphical neural network in that graph sage and vgae is used also time stamp condition is also considered
